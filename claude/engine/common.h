#ifndef _COMMON_H_
#define _COMMON_H_

/* Shared across the whole engine. Kept free of host-only headers so this
   tree can eventually be recompiled with cl65 for the X16 UI layer. */

typedef unsigned char byte;
typedef unsigned int  word;

#endif
